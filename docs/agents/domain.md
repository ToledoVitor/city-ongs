# Domain documentation

Use root `CONTEXT.md` for shared domain language and `docs/adr/` for architectural decisions. Create either location only when domain-model or decision work needs it.

Product capabilities remain documented in `FUNCIONALIDADES_SISTEMA.md`; product and visual rules remain in `DESIGN.md`.
